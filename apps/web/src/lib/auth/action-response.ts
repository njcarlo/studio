/**
 * @deprecated Import from `@studio/core-engine` instead.
 * Re-export shim — action response helpers live in packages/core-engine (Slice A).
 */
export {
  ok,
  err,
  toErrorMessage,
  type ActionSuccess,
  type ActionError,
  type ActionResponse,
} from '@studio/core-engine';
