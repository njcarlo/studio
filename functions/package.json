{
  "name": "functions",
  "version": "1.0.0",
  "private": true,
  "description": "Firebase Cloud Functions — HTTP API + scheduled jobs (ported from the former Supabase Edge Functions).",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "gcp-build": "prisma generate --schema=prisma/schema.prisma",
    "typecheck": "tsc --noEmit",
    "serve": "npm run build && firebase emulators:start --only functions",
    "prisma:generate": "mkdir -p prisma && cp ../prisma/schema.prisma prisma/schema.prisma && prisma generate --schema=prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "express": "^4.21.2",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.4.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "prisma": "^5.22.0",
    "typescript": "^5"
  }
}
